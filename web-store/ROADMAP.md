# Roadmap доработок сайта БытТехОпт (climat-simf.ru)

> Составлено 2026-06-23 после мобильного аудита кабинета (Iter 65). Приоритет — по
> влиянию на продажи (это магазин). Отметки: 🟢 дёшево · 🟡 средне · 🔴 большой проект.
> `[уже долг]` — было в CLAUDE.md/HANDOFF; `[аудит]` — замечено в этой сессии.
> Многое можно измерить на проде/телефоне CDP-инструментом (`qa-cabinet-audit/*.mjs`) —
> решать по цифрам, а не на глаз.

## 🥇 Tier 1 — Деньги / конверсия (важнее всего)

- **1. Фото товаров — покрытие** 🔴 `[уже долг]` — ЧАСТИЧНО (Iter 67, 2026-06-24)
  - [x] ✅ Восстановлено фото у **~19 168** товаров (поставщик их отдавал, синк не дотягивал из-за веб-таймаута) → «без фото» **44% → 34%**. Скрипт `scripts/recover-gap-images.ts`.
  - [x] ✅ Честный фильтр «только с фото» (по реальному изображению, не флагу `hasImage`) — `5448648`.
  - [x] ✅ **Системно:** cron `30 6 * * *` на `recover-gap-images.ts` на VPS (2026-06-25) — gap не соберётся заново для новых товаров. Лог `/var/log/img-gap-recover-cron.log`.
  - [ ] 🔴 **~66k товаров** реально без фото у поставщика → внешний источник (бизнес-решение). См. `PHOTO-CONTENT-SPEC.md`.
- **2. Скорость** `[уже долг]`
  - [x] ✅ **HTTP/2** включён (Iter 66, 2026-06-24) — `listen 443 ssl http2;`, проверено ALPN h2.
  - [x] ✅ **WebP + resize** фото (Iter 66) — `/api/product-images` отдаёт WebP ≤800 через sharp; замер ×12.5 (45.7КБ→3.7КБ). Кэш nginx сброшен.
  - [x] ✅ **Cold-start** (Iter 68, 2026-06-24): TTL листингов `600→3600` (под прогрев) + прогрев кэша после деплоя (`warm_cache`+`warm_all` в `deploy_vps.py`). holodilniki 13с(cold)→1.15с. Установившийся режим был ~1с; укус — окно после деплоя/истечения TTL, теперь закрыто.
- [x] ✅ **3. Характеристики на карточке товара** 🟡 `[уже долг F2]` — ЧАСТИЧНО (Iter 70, 2026-07-01)
  Расширен парсер (`lib/product-attributes.ts`) для 5 категорий с максимальным разрывом × объёмом:
  розетки/выключатели/рамки (48.9%→31.5% бедных), варочные панели (86.8%→47.6%, был `\w`-баг гейта),
  роутеры/коммутаторы (87.0%→61.9%, новое семейство `network`), камеры (81.8%→49.1%), кулеры ПК
  (72.5%→54.3%). Бэкфилл на проде подтверждён psql. Остальные ~66k товаров-расходников (картриджи/
  чипы/дисплеи-запчасти) намеренно вне скоупа — их названия не содержат парсибельного текста.

## 🥈 Tier 2 — Замкнуть кабинет (auth уже работает)

- [x] ✅ **4. История заказов в `/account`** (Iter 69, 2026-06-25) — `Order.userId` (миграция) +
  checkout проставляет userId + карточка «История заказов» (номер/дата/статус/сумма/ссылка).
  Проверено на телефоне (пустое состояние, 0 ошибок). Долг: b2b/gov КП-история — отдельно.
- [ ] **5. Реальные опт-цены b2b** 🔴 `[уже долг]`
  В схеме нет `wholesalePrice`, сейчас наценочная модель `retail × (1−%)`. Настоящий
  опт = миграция схемы + синк с поставщиком.

## 🥉 Tier 3 — Полировка / доверие

- [ ] **6. Футер: соцсети Telegram/WhatsApp = `href="#"`** 🟢 `[уже долг]` — реальные ссылки или скрыть.
- [ ] **7. Сравнение на 360px** 🟢 `[аудит]` — 4 колонки тесно, колонка характеристик уезжает при горизонт. скролле → **sticky первая колонка** (`.compare-th-label`/`.compare-row-label`).
- [ ] **8. Фильтр «в наличии» (C5)** 🟡 `[уже долг]` — два состояния склада (сейчас только «доступно к заказу»); требует stock-модели.

## 🛠 Tier 4 — Операционка / надёжность

- [ ] **9. Аудит админки** 🟡 `[уже долг]` — владелец её **ни разу не смотрел**. Пройти все разделы (заказы/лиды/заявки/товары/настройки), проверить UX. Отдельная итерация.
- [ ] **10. Доставляемость почты на Gmail** 🟡 `[уже долг]` — письма в спам (репутация IP/домена; вся аутентификация проходит). Прогрев + Google Postmaster. Нет `fail2ban`/антиспама на почтовом сервере.

---

### Рекомендованный старт
- ~~Быстрый win для скорости: #2 (HTTP/2)~~ — сделано.
- ~~Главный, но большой: #1 (фото)~~ — сделано частично, остаток требует внешнего источника.
- ~~«Оживить» кабинет: #4 (история заказов)~~ — сделано.
- ~~#3 (характеристики на карточке)~~ — сделано частично (Iter 70), остаток вне скоупа (расходники).
- Дальше: **#7 (sticky-колонка сравнения)** — дёшево, без внешних данных, можно закрыть быстро;
  **#6 (футер-соцсети)** — дёшево, но нужны реальные ссылки от владельца или решение скрыть.
