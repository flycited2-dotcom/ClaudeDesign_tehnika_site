# Iter 15B — Real /service page (warranty + installation pricing + ASC list)

**Дата:** 2026-05-16
**Статус:** Утверждён к реализации (брейнсторм, брифинг от пользователя)
**Зависимый коммит:** `9a0536f` (Iter 15A) — поверх него

## Цель

Заменить coming-soon заглушку `/service` на реальную страницу с тремя разделами:
гарантия от производителя, прайс на установку силами своих мастеров, список
авторизованных сервисных центров для ремонта (мы сами не ремонтируем).

## Брифинг от пользователя

**Установка** (свои мастера, весь Крым, выезд в течение 24 часов, гарантия на работы 12 мес):

| Категория | Цена |
| --- | --- |
| Стиральная машина | от 2 500 ₽ |
| Холодильник | от 2 500 ₽ |
| Газовая плита | от 2 500 ₽ |
| Сплит-система | от 8 000 ₽ |
| Духовка | от 3 000 ₽ |
| Варочная панель | от 3 000 ₽ |
| Посудомойка | от 3 000 ₽ |
| Вытяжка | от 3 000 ₽ |
| Бойлер | от 3 000 ₽ |

**Ремонт:** мы не ремонтируем, рекомендуем авторизованные СЦ:

| Название | Адрес | Телефоны | Сайт |
| --- | --- | --- | --- |
| НК-Центр | ул. Ладыгина, 21 | +7 (978) 066-24-01, +7 (3652) 54-45-73 | nk-center.ru |
| СЦ Максимум | ул. Генерала Васильева, 30 | +7 (978) 127-31-21 | — |
| Гарант Сервис | ул. Дзюбанова, 49 | +7 (978) 722-74-79 | garantsimf.ru |

**Гарантия от производителя:** заводская, 12 мес мелкая БТ, 24–36 мес крупная БТ/климат. Для гарантийного случая нужны чек/УПД, гарантийный талон, описание неисправности.

## Архитектура

Один server-page, никаких новых компонентов, никакого нового CSS. Используем
существующие классы из glass-template (`.glass`, `.bread`, `.section-head`,
`.p-card`, `.btn-primary`) и существующий `<CallbackButton variant="inline">`
для CTA «Заказать установку».

### Файл

#### `src/app/service/page.tsx` — ПЕРЕПИСАТЬ (server)

- Удалить stub-контент (карточку с email).
- `metadata`: убрать `robots: { index: false }`, обновить `description` под
  реальный контент с ключевиками («установка бытовой техники Симферополь Крым»).
- Структура:
  ```
  <div className="bread">Главная › Сервис и установка</div>
  <div className="section-head">
    <h2>Сервис и установка</h2>
    <div className="meta">Гарантия, установка по Крыму, авторизованные сервисные центры</div>
  </div>

  <section> Гарантия от производителя — .glass карточка </section>
  <section> Установка — intro + 3×3 grid карточек цен + CallbackButton </section>
  <section> Ремонт — intro + 3-колоночная сетка карточек АСЦ </section>
  ```

### Иконки (lucide-react)

| Категория | Иконка |
| --- | --- |
| Стиральная машина | `WashingMachine` |
| Холодильник | `Refrigerator` |
| Газовая плита | `Flame` |
| Сплит-система | `Snowflake` |
| Духовка | `Microwave` |
| Варочная панель | `CookingPot` |
| Посудомойка | `UtensilsCrossed` |
| Вытяжка | `Wind` |
| Бойлер | `Droplet` |
| Гарантия (раздел) | `ShieldCheck` |
| Ремонт (раздел) | `Wrench` |
| Установка (раздел) | `Settings2` |

Все имена существуют в lucide-react.

### Сетка карточек цен

CSS-grid через inline style (без нового CSS):
`grid-template-columns: repeat(auto-fill, minmax(220px, 1fr))`, `gap: 16`.
Карточка — `.p-card` без интерактива (без `.p-card-link`).

### Сетка карточек АСЦ

`grid-template-columns: repeat(auto-fill, minmax(280px, 1fr))`, `gap: 16`.
Карточка — `.glass` с padding. Телефоны → `<a href="tel:...">`, сайт →
`<a href="https://..." target="_blank" rel="noopener noreferrer">`.

## Verification

1. `npm run lint` — без ошибок.
2. `npm run test` — 134/134 (изменений в тестируемой логике нет).
3. `npm run build` — успешен.
4. Prod-smoke `/service` → 200, в HTML — заголовки разделов, цены «от 2 500 ₽», телефон «+7 (978) 066-24-01», текст «НК-Центр».
5. HANDOFF entry.

## Что НЕ входит

- Своя форма заявки на сервис (используем CallbackButton — он уже работает через Telegram-уведомление менеджеру).
- Динамический парсинг прайса (всё статикой в page.tsx, легко править).
- Картинки СЦ / логотипы брендов.
