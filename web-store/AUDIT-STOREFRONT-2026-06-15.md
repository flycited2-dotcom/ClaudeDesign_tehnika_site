# Аудит витрины climat-simf.ru — 2026-06-15

> Сквозной аудит 5 агентами (главная, каталог+карточки, навигация, прочие страницы,
> инвентаризация изображений). Приоритеты: P0 — грубо/ломает/первый экран; P1 — заметно;
> P2 — полировка. Фото-слоты вынесены в `PHOTO-CONTENT-SPEC.md`.

## Сквозные / самые важные

- **[P0] Нижняя нав-плашка `SiteScreenBar` показывается на ДЕСКТОПЕ** — `globals.css:998-1053` +
  `glass-template.css:125-136`. Нет `@media (min-width:761px){.screen-bar{display:none}}`. Перекрывает
  нижний контент, дублирует навигацию (вся есть в шапке), на планшете (761–1100px) даёт горизонтальный
  оверфлоу. **Фикс:** скрыть на ≥761px + обнулить десктопный `body.app{padding-bottom}`. Одна правка
  закрывает 3 находки (SF3-A-1/A-3).
- **[P0] Hero главной — штрихованный плейсхолдер с одиноким SVG-холодильником** (`page.tsx:80-83`).
  Первый экран премиум-магазина буквально выглядит как «здесь будет картинка». В репо уже лежит
  неподключённый реальный фон `public/assets/hero-showroom.png` — оценить/подключить.
- **[P0] Плитки категорий — пустые коробки 300px** (`page.tsx:124-138`): рендерится только текст,
  слот `.cat-art` из эталона не выводится → огромная пустота снизу у 6 карточек над линией прокрутки.
- **[P0] Единая заглушка «нет фото»** существует в 3 несогласованных видах (карточка/детальная/API).
  ~40% товаров без фото — заглушка очень заметна. См. `PHOTO-CONTENT-SPEC.md §1.6`.
- **[P1] Брейкпоинты шапки рассогласованы** (`760/900/1100`) — на 901–1100px мега-меню есть, бургера нет,
  всплывает вторая строка поиска; «чистая» только конфигурация ≤760px. Свести к единой паре порогов.

## Главная (SF1)

- [P1] Промо «Кофемашина DeLonghi» — хардкод товара/цены, «Подробнее»→`/catalog`, корзина-иконка→`/cart`
  (ничего не кладёт) (`page.tsx:141-164`). Привязать к реальному товару либо обобщить.
- [P1] Промо «Умный дом» — фейк-телефон из пустых плиток, CTA на общий `/catalog` (`page.tsx:166-202`).
- [P1] Секции `#how-order` и `#contacts` целиком на inline-стилях (`page.tsx:205-396`) — невозможны
  media-query, хардкод-цвета `#75a2ff/#426dff` мимо токенов. Вынести в классы эталона.
- [P2] Необоснованные обещания: «Скидки до 35%», «Рассрочка 0%» — нет механики/посадочной (`page.tsx:53,101`).
- [P2] «Более 8 500 моделей» — хардкод, расходится с БД (`page.tsx:116`).
- [P2] Адаптив hero: `<Fridge size={300}>` фикс, плавающие карточки на абсолюте перекрываются на планшете.

## Каталог и карточка товара (SF2)

- (см. отчёт SF2) 3 рассинхронных плейсхолдера фото → единый; `hasImage` врёт у ~3% → нужен `onError`;
  мобильная карточка тесная; «Цена уточняется»/RRP оформление; проверить фильтры/сортировку/пагинацию,
  хлебные крошки, ролевые цены b2c/b2b/gov, адаптив сетки.

## Навигация (SF3)

- [P1] Футер: форма подписки `action="#"` — мёртвый submit (`site-footer.tsx:19-22`).
- [P1] Футер без навигационных ссылок; `/privacy` недостижима на десктопе (только в моб. бургере).
- [P2] Второй телефон `phones[1]` нигде не выводится (`site-header.tsx:422`, `lib/storefront.ts:5`).
- [P2] Мега-меню: категории без иконок (опц. улучшение сканируемости); пустое состояние не кликабельно.
- [P2] Поиск: пустой результат подсказок без ссылки на `/catalog`.

## Прочие страницы (SF4)

- [P1] **Противоречие оплаты:** checkout-метаданные «Оплата при получении» vs тело «после подтверждения»
  (`checkout/page.tsx:8` vs `checkout-client.tsx:51`).
- [P1] **Нет сводки заказа на /checkout** (только форма) — пользователь теряет состав/сумму. Есть
  неиспользуемый класс `.checkout-grid`.
- [P1] **b2b/gov hero-баннер не стекается на мобильных** (`.b2b-banner` отсутствует в media-блоке).
- [P1] **Подборки** = голый CatalogView без обложки/описания, хотя это `index:true` SEO-лендинги.
- [P1] **/order-success/[id]** доступна по сырому ID без `noindex` (утечка состава/суммы чужого заказа).
- [P1] **/privacy** без реквизитов оператора и даты (юр. пробел при форме согласия на checkout).
- [P2] Эмодзи-иконки в «Быстрых действиях» account вместо lucide (`account/page.tsx:81-87`).
- [P2] Опечатка «переферия»→«периферия» (`b2b/page.tsx:23`).
- [P2] Иконка `LogOut` у «Связаться с менеджером» (семантика; `account-shell.tsx:134`).
- [P2] Разнобой радиусов вне шкалы токенов (16/20/28) по cart/checkout/service/order-success.
- [P2] `force-dynamic` + `revalidate=300` конфликтуют в podborki; `force-dynamic` на главной.

## Изображения

Полностью вынесено в **`PHOTO-CONTENT-SPEC.md`**: ~6–8 уникальных дизайнерских фото
(hero / 2 промо / 2 баннера / единая заглушка / OG (+опц. обложки)) + товарный per-SKU конвейер
(144 486 без фото). Найден неподключённый `public/assets/hero-showroom.png`.
